<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.jpnstudy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WordTestActivity" />
        <activity android:name=".WordListActivity" />
        <activity android:name=".TestLevelSelect" />
        <activity android:name=".YoutubeActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".WebPage" />
        <activity android:name=".Favorites" />
        <activity android:name=".Word" />
        <activity android:name=".YoutubeList" />
        <activity android:name=".YoutubeListData" />
        <activity android:name=".Settingg"></activity>
    </application>

</manifest>